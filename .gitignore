# Secrets
.env
google-routes-config.js

# Dependencies and local caches
node_modules/
.cache/
vendor/

# OSM downloads (multi-GB) — only PBF stays local
osm-pbf/

# Other POI ingest caches (camping, scenic, playgrounds, etc.)
*-ingest/
!fuel-us-ingest/
!fuel-us-ingest/**
!fuel-ca-ingest/
!fuel-ca-ingest/**

# Optional per-state explorer slices (regenerate locally)
fuel-explorer-cache/

# Logs and backups
*.log
*.pre-filter-backup.json
desktop.ini
Thumbs.db
